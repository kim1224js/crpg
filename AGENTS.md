# CRPG 프로젝트 작업 규칙

이 문서는 이 저장소에서 작업하는 Codex 및 자동화 도구가 따라야 하는 프로젝트 지침이다. 저장소 루트부터 모든 하위 폴더에 적용한다.

## 1. 기본 원칙

- 사용자의 최신 요청을 최우선으로 따른다.
- 새 시스템, 아이템, 몬스터, 수치 또는 연출처럼 기획 판단이 필요한 내용을 추가하려면 적용 전에 사용자에게 확인한다.
- 사용자가 요청하지 않은 기능 확장, 대규모 구조 변경, 의존성 교체는 하지 않는다.
- 수정은 기존 기능과 사용자 데이터를 보존하는 방향으로 진행한다.
- APK와 AAB는 사용자가 명시적으로 요청했을 때만 생성한다.
- 최신 정보에 따라 결과가 달라질 수 있는 Android SDK, 라이브러리, 정책 또는 외부 자료는 현재 공식 정보를 확인한 뒤 적용한다.

## 2. 프로젝트 개요

- Android 단일 앱 모듈 프로젝트이며 Kotlin, Android View, Room DB, KSP를 사용한다.
- 애플리케이션 ID와 네임스페이스는 `es.kim.crpg`이다.
- 현재 SDK 설정은 `compileSdk 36.1`, `minSdk 36`, `targetSdk 36`이다.
- Java 호환 버전은 11이다.
- Gradle 버전 카탈로그와 Kotlin DSL을 사용한다.
- 출시 빌드의 R8 최적화는 현재 비활성화되어 있다.

## 3. 주요 코드 위치

- 앱 진입과 화면 전환: `app/src/main/java/es/kim/crpg/MainActivity.kt`
- Room DB, 초기 데이터와 마이그레이션: `app/src/main/java/es/kim/crpg/data/`
- 아이템 정의와 공통 옵션 변환: `app/src/main/java/es/kim/crpg/game/catalog/`
- 게임 규칙: `app/src/main/java/es/kim/crpg/game/rules/`
- 던전 전투와 렌더링: `app/src/main/java/es/kim/crpg/ui/dungeon/`
- 공통 인벤토리: `app/src/main/java/es/kim/crpg/ui/inventory/`
- 공통 팝업과 스크롤 UI: `app/src/main/java/es/kim/crpg/ui/common/`
- 시설 화면: `app/src/main/java/es/kim/crpg/ui/facility/`
- 설정과 가이드: `app/src/main/java/es/kim/crpg/ui/settings/`
- 이미지와 오디오: `app/src/main/assets/ui/`, `app/src/main/assets/audio/`
- 개발자 게임 문서: `app/src/main/assets/book.txt`, `app/src/main/assets/game_book/`

## 4. 데이터와 저장 규칙

- 아이템, 몬스터, 드랍률, 수량, 층별 배치, 상호작용 구조물과 조절 가능한 게임 수치는 가능한 한 Room DB를 기준으로 관리한다.
- 같은 게임 데이터를 여러 화면에 숫자나 문자열로 중복 하드코딩하지 않는다.
- DB의 아이템 정의는 `ItemCatalog`를 통해 UI와 전투가 함께 사용하도록 유지한다.
- DB Entity 또는 테이블 구조를 변경하면 반드시 DB 버전을 올리고 순차 Room 마이그레이션을 추가한다.
- 새 DB 스키마는 `app/schemas/es.kim.crpg.data.GameDatabase/`에 내보내고 Git 관리 대상에 포함한다.
- 기존 설치 사용자의 로그인, 보유 아이템, 창고, 장착 상태, 최고 층, 사망 기록과 진행 중 던전 저장을 잃지 않도록 한다.
- 던전 중 상태가 바뀌는 기능은 종료 후 재개해도 복원되는지 확인한다.
- 고급 이상 장비의 미확인·감정 규칙과 소모품의 즉시 사용 가능 규칙을 혼동하지 않는다.

## 5. UI와 게임 규칙

- 기존 엔틱 중세·다크 판타지 디자인을 유지한다.
- 반복되는 팝업은 `AntiqueGameDialog`, 반복되는 스크롤은 공통 스크롤 구성요소를 우선 사용한다.
- 인벤토리 슬롯과 장비 정보 UI는 공통 컴포넌트를 사용하고 화면별 복제 구현을 피한다.
- 목록이 화면 높이를 넘을 가능성이 있으면 실제 목록 영역에 스크롤을 적용한다. 상위 전체 화면 스크롤 때문에 버튼이나 탭이 함께 사라지지 않게 한다.
- 터치 영역, 텍스트, 이미지, 팝업 버튼이 겹치거나 화면 밖으로 잘리지 않는지 확인한다.
- 전투는 1턴 1행동 규칙과 몬스터 순차 행동 연출을 유지한다.
- 전투 속도 변경, 자동 이동, 카메라 포커싱 중에도 캐릭터·몬스터·투사체·전리품이 보이지 않게 되는 회귀를 확인한다.
- 장비 효과는 실제 장착 여부와 공격 유형을 기준으로 판정한다. 소지하고 있다는 이유만으로 장착 효과를 적용하지 않는다.

## 6. 에셋과 문서

- 새 이미지와 오디오는 프로젝트의 기존 폴더 구조와 이름 규칙에 맞춰 저장한다.
- 인벤토리 아이콘은 작은 크기에서도 물체의 실루엣과 등급 구분이 선명해야 한다.
- 에셋 경로를 DB에 저장했다면 파일 존재 여부와 대소문자를 확인한다.
- 외부 에셋은 라이선스를 확인하고 필요한 라이선스 문서를 함께 보관한다.
- 아이템, 몬스터, 던전, 이벤트 또는 핵심 규칙을 변경하면 `book.txt`와 관련 `game_book/*.txt` 문서도 함께 갱신한다.
- 앱 내 장비·몬스터·이벤트 도감이 DB 내용과 일치하는지 확인한다.

## 7. 코드 구성

- `MainActivity`와 `DungeonDemoView`에 기능을 무조건 추가하지 말고, 재사용 가능한 UI·규칙·데이터 접근은 역할별 파일로 분리한다.
- 공통 로직을 화면마다 복사하지 않는다.
- 함수는 한 가지 책임을 갖도록 유지하고, 상태 이름은 효과와 지속 조건이 드러나게 작성한다.
- 관련 없는 사용자 수정이나 작업 중인 파일을 되돌리지 않는다.
- 불필요한 전체 포맷팅과 대규모 기계적 변경은 피한다.

## 8. 검증 절차

- 코드 변경 후 우선 `./gradlew.bat compileDebugKotlin testDebugUnitTest`를 실행한다.
- Room 스키마 변경 후 새 스키마 JSON 생성 여부와 마이그레이션 등록 여부를 확인한다.
- 실패가 Gradle 중간 생성물 오류로 확인된 경우에만 `clean` 후 다시 검증한다.
- 기능 위험도에 따라 로그인, 마을, 상점, 창고, 장착, 던전 진입, 전투, 저장·복원 흐름을 함께 점검한다.
- 컴파일 경고와 실제 실패를 구분하여 보고한다.
- 사용자가 요청하지 않았다면 검증을 위해 APK를 만들지 않는다.

## 9. Git과 보안

- 커밋이나 푸시는 사용자가 명시적으로 요청했을 때만 수행한다.
- 커밋 전에 `.gitignore`와 변경 파일을 확인하고 비밀키, 서명키, 로컬 설정, 계정 정보, DB 파일과 빌드 산출물을 제외한다.
- 기존 변경 사항은 사용자 작업으로 간주하고 보존한다.
- 파괴적인 Git 명령은 사용하지 않는다.
- 원격 저장소 인증 계정과 대상 브랜치를 푸시 전에 확인한다.
